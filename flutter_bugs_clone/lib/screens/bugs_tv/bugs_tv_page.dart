import 'package:flutter/material.dart';

class BugsTvPage extends StatelessWidget {
  const BugsTvPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("BugsTvPage"),
    );
  }
}
