import 'package:flutter/material.dart';

class EverytimePage extends StatelessWidget {
  const EverytimePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("EverytimePage"),
    );
  }
}
